# GrafTrail v1.5.1 - Cross-Platform Mouse Trail Application
# Requirements for building executables

# Core dependencies
PyQt5>=5.15.0,<6.0.0        # GUI framework (cross-platform)
pyautogui>=0.9.54           # Mouse position detection (cross-platform)

# Build dependencies
pyinstaller>=5.13.0         # Executable creation
pillow>=9.0.0              # Image processing for icons

# Platform-specific dependencies
# Windows: pywin32 (included with PyQt5)
pyobjc-framework-Cocoa>=9.0.0; sys_platform == "darwin"  # macOS global key detection
# Linux: python3-tk (system package)

# Development dependencies (optional)
# black>=23.0.0             # Code formatting
# flake8>=6.0.0             # Linting
